
import { theme } from './theme'; // Added import for theme
// src/styles/index.ts - Main styles export
export { theme } from './theme';
export { globalStyles } from './globalStyles';
// Convenience exports for commonly used values
export const { colors, spacing, fontSize, fontWeight, borderRadius, shadows } = theme;